import { Project, Opportunity } from '../types';

// Data cleared as requested. The app is now ready for real data entry.
export const MOCK_PROJECTS: Project[] = [];

export const MOCK_OPPORTUNITIES: Opportunity[] = [];
